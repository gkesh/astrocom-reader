---
const { text, href, img } = Astro.props;
---
<article onclick={`location.href='${href}'`}>
    <img src={img} alt={text}>
    <span>{text}</span>
</article>

<style lang="scss">
article {
    overflow: none;
    border-radius: var(--border-radius);
    position: relative;
    aspect-ratio: 1;

    img {
        width: 100%;
        height: 100%;
    }

    span {
        position: absolute;
        bottom: 0;
        right: 0;
        left: 0;
        width: 100%;
    }
}
</style>